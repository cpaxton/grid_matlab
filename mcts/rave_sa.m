function q = rave_sa(s,a, S, A, R, n)
% Compute continuous RAVE score from state/action/reward information.
% All states, actions must be stored in MctsNode object in order to do
% this.
% Also, we need to store the number of times each state/action has been
% visited.



end

% distance function is a simple euclidian distance metric
function d = dist(x,y)

end